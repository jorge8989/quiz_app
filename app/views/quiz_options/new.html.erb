<h1>New Quiz Option</h1>

<%= form_for [@question, @quiz_option] do |f| %>
  <% if @quiz_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz_option.errors.count, "error") %> prohibited this quiz_option from being saved:</h2>

      <ul>
      <% @quiz_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', question_path(@question) %>
